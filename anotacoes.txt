react scripts tem um conjunto de conf pro react ser entendido pelo browser. Internamente ele tem o Babel e o Webpack

router-dom é o modulo mais para fazer navegação, controlar o componente ativo baseado na navegação.

nas rotas o react verifica o path como um include, usamos o exact para verifica igualdade (pois apenas no include, podemos ter componentes repetidos)

pra declarar as tipagens com typescripts interface (define o formato das tipagens) <Nome>Props{}

node é non-block IO, existe um controle com asyncronism. EX: dá pra enviar o email e cadastrar um user ao mesmo tempo

ts-node-dev executa o server e fica observando

flag --transpile-only faz com que ele só faça a transpilação e não fique verificando erros

flag --ignore-watch node_modules evita converter códigos de lá

flag --respawn toda vez que alterar o cod, restart e só para com o ctrl+c

rota é o endereço da aplicação

recurso é o /users 

insert permite inserir varios users, logo retorna uma lista de Ids

transaction é fazer todas as operações do banco ao mesmo tempo, e se uma delas falhar, desfazer todas ao mesmo tempo

apenas no trx.commit que ele insere tudo no db (ou não)

damos um trx.rollback no catch para desfazer alterações que foram feitas até cair nele

React Native não trabalha com classes/id/classes

Native não funciona herança, estilo do item no item, exceto a tag Text

React não lê svg, teria que instalar um pacote para isso

Native lida automaticamente com a densidade de pixels da tela, (busca pelo @ da tela @2x, @3x)

resizeMode redimensiona  a imagem proporcionalmente a altura/largura 
  *contain:e todo o conteudo visivel
  *cover:corta a imagem para acontecer

{'\n'} para quebra de linha

expo google fonts para importamos fonts no react native

AppLoading é um componente do expo que mostra o mesmo conteudo de uma splash,usaremos ele até as fontes estarem carregadas


dentro do native não tem variaveis do css, mas daria pra usar variaveis do JS

criamos uma pasta Routes pois usaremos mais de um tipo de navegação (stack e em abas)

NavigationContainer tipo o BrowserRouter, tem que ficar em volta de todas as rotas

elevation é a propriedade de sombras no android

quando queremos colocar o source de uma img externa usamos o uri

no interface, o ReactNode nos permite receber um componente como props

diferente do web, usamos como base_url um ip acessível pela rede, daí consegue usar seu celular e o emulador (visto que não pega o localhost)

deep linking = uma aplicação abrir outra

